import { ISODuration } from '../types';
/**
 * @ignore
 */
export declare class Parser {
    static parseUrl(url: string): {
        video: string;
        playlist: string;
        channel: string;
    };
    static parseIsoDuration(duration: string): ISODuration;
}
